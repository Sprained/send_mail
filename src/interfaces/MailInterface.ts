export interface IUpdateInfosMail {
  awsAccessKeyId?: string,
  awsSecretAccessKey?: string,
  host?: string,
  port?: number,
  secure?: boolean,
  user?: string,
  pass?: string
}

export interface INodemailerInfosUp {
  host?: string,
  port?: number,
  secure?: boolean,
  user?: string,
  pass?: string
}

export interface IAWSInfosUp {
  awsAccessKeyId?: string,
  awsSecretAccessKey?: string
}